library(radmac2);
context('Length of Stay');

z<-los(db='emdm2016a',sim='emdm2016a',protocol='Geyserville');

test_that('q75 greater than mean', {
              expect_gt(z$q75,z$medians)
          })
